import styles from "./Settings.module.css";

function Settings() {
  return (
    <section className={styles.page}>
      <h1>Settings</h1>
      <p>Settings components will be added next.</p>
    </section>
  );
}

export default Settings;
